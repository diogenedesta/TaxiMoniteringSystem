@model IEnumerable<Taxi_Monitering.tbl_Role>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Role_Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Role_Permission)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Role_Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Role_Permission)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Role_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Role_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Role_ID })
        </td>
    </tr>
}

</table>
